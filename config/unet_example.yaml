model:
  in_channels: 1
  out_channels: 1
  base_channels: 32
  bilinear: true

data:
  train_root: "data/train"
  val_root: "data/val"
  image_size: [256, 256]

train:
  epochs: 50
  batch_size: 4
  lr: 0.001
  weight_decay: 0.0

device: "cuda"   # or "cpu"
output_dir: "runs/example"